# Issue tracker

This repo uses **GitHub Issues**.

## Creating issues

Use the `gh` CLI:

```bash
gh issue create --title "..." --body "..."
```

## Required environment

- `gh` CLI installed and authenticated
- Write access to `rongtianhua/autodock`
